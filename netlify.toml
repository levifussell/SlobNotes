[build]
  command = "git clone $NOTES_REPO /tmp/notes && pip install cryptography && NOTES_ROOT=/tmp/notes python3 tool/scripts/build_static.py"
  publish = "tool/dist"

[build.environment]
  PYTHON_VERSION = "3.12"
